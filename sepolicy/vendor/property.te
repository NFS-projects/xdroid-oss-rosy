# fingerprint prop
vendor_public_prop(hal_fingerprint_prop);

# Power HAL
vendor_internal_prop(vendor_power_propg);

# wlan-vendor prop
vendor_internal_prop(vendor_wifi_prop);

# NFSparts dirac
type dirac_prop, property_type;
